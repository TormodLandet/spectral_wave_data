name: "Build Python Wheel on Windows"

on: [push, pull_request]

env:
  # Cortran compiler, see https://github.com/marketplace/actions/setup-fortran
  fortran_compiler: "intel-classic"
  fortran_compiler_version: "2021.12"
  # Python versions for uv, https://docs.astral.sh/uv/concepts/python-versions/
  python_version_build: "3.13"
  python_version_test: "3.10"

jobs:
  build_wheels:
    name: Build Windows Wheel using Intel oneAPI Fortran Compiler (ifort)
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: fortran-lang/setup-fortran@v1
        id: setup-fortran
        with:
          compiler: ${{ env.fortran_compiler }}
          version: ${{ env.fortran_compiler_version }}

      - name: Show compiler information
        shell: cmd
        run: |
          ECHO ...... VARS .........................................................................
          ECHO PATH=%PATH%
          ECHO FC=%FC%
          ECHO CC=%CC%

          ECHO env.FC=${{ env.FC }}
          ECHO env.CC=${{ env.CC }}

          ECHO fortran_compiler=%fortran_compiler%
          ECHO env.fortran_compiler=${{ env.fortran_compiler }}

          ECHO fortran_compiler_version=%fortran_compiler_version%
          ECHO env.fortran_compiler_version=${{ env.fortran_compiler_version }}

          ECHO ...... Run ifort without an input file ..............................................
          ifort

          ECHO ...... Run cl without an input file .................................................
          cl

          ECHO ...... Run FC without an input file .................................................
          ${{ env.FC }}

          ECHO ...... Run CC without an input file .................................................
          ${{ env.CC }}

          ECHO ...... Run CMake with the --version flag ............................................
          cmake --version

          ECHO ...... END OF TESTING ...............................................................

      - name: Compile the SWD Fortran library
        shell: cmd
        run: |
          CD src\api\python\Cmake
          RMDIR /s /q Build_Win64
          MKDIR Build_Win64
          CD Build_Win64

          ECHO .....................................................................................
          ECHO ..... CMake: Generate the Visual Studio build files .................................
          cmake -G "Visual Studio 17 2022" -A x64 -DCMAKE_WINDOWS_EXPORT_ALL_SYMBOLS=TRUE ..
          IF %ERRORLEVEL% NEQ 0 (
            ECHO Error occurred during CMake build generation
            GOTO :on_error
          )

          ECHO .....................................................................................
          ECHO ..... CMake: Compile using the Release configuration ................................
          cmake --build . --config Release
          IF %ERRORLEVEL% NEQ 0 (
            ECHO Error occurred during CMake build compilation
            GOTO :on_error
          )

          ECHO .....................................................................................
          ECHO ..... Debug: List the generated files ...............................................
          DIR /S

          EXIT /B 0

          :on_error
          ECHO An error occurred during the build process
          EXIT /B 1

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.8.3"
          python-version: ${{ env.python_version_build }}
          enable-cache: true

      - name: Build wheel
        shell: powershell
        run: |
          # Stop the script when a cmdlet or a native command fails
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true

          cd src\api\python

          # Install auditwheel and any other dependencies
          uv sync --only-group=build

          # Create the wheel file (can also use "pip wheel .")
          # The file ends up in the "dist" directory
          uv build --wheel

          Get-ChildItem -Path dist -Recurse -Force

      - name: Install the wheel
        shell: powershell
        run: |
          # Stop the script when a cmdlet or a native command fails
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true

          cd tests\python

          # Create a virtual environment with the oldest supported Python version
          uv venv .venv --clear --no-project --seed --python ${{ env.python_version_test }}
          .\.venv\Scripts\Activate.ps1

          # Install the test dependencies and then the newly built manylinux wheel
          pip install -r requirements_tests.txt
          pip install ..\..\src\api\python\dist\spectral_wave_data*.whl

          # Show the installation information with console scripts
          pip show --verbose spectral_wave_data
          Get-ChildItem -Path .venv\Scripts -Recurse -Force

          # Show the installed package version
          python -c "import spectral_wave_data; print(spectral_wave_data.version_full)"
          swd_meta --version

      - name: Test the wheel
        shell: powershell
        run: |
          # Stop the script when a cmdlet or a native command fails
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true

          cd tests\python
          .\.venv\Scripts\Activate.ps1

          # Run a subset of the tests to verify that the wheel is functional
          $env:SWD_TEST_TYPE="quick"
          python -m pytest -v

      - name: Upload Python wheels
        uses: actions/upload-artifact@v4
        with:
          name: python-wheel-win64
          path: src/api/python/dist
